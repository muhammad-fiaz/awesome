---
import AppShell from './AppShell.astro';
import { RightSidebar } from '@/components/layout/RightSidebar';
import { buildToc } from '@/lib/toc';
import type { TocItem } from '@/types';

export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  headings?: Array<{ depth: number; slug: string; text: string }>;
  postLinks?: Record<string, string | undefined>;
  relatedLinks?: Array<{ label: string; href: string; external?: boolean }>;
  keywords?: string;
}

const {
  title,
  description,
  ogImage,
  headings = [],
  postLinks = {},
  relatedLinks = [],
  keywords,
} = Astro.props;

const toc: TocItem[] = buildToc(headings);
---

<AppShell title={title} description={description} ogImage={ogImage} keywords={keywords}>
  <Fragment slot="head">
    <meta property="og:type" content="article" />
    <slot name="head" />
  </Fragment>
  <div class="w-full xl:pr-64">
    <article class="w-full min-w-0 max-w-5xl mx-auto px-4 sm:px-6 md:px-8 xl:px-10">
      <slot />
    </article>
    <RightSidebar
      client:load
      toc={toc}
      postLinks={postLinks}
      relatedLinks={relatedLinks}
    />
  </div>
</AppShell>
